extends Fillabe_Bar



	#loop through the players, find the player that you have control over and start listening to the health change signal
	# for player in ArenaState.players:
		# if player.is_network_master():
			# player.health_change_signal.connect(update_bar)
